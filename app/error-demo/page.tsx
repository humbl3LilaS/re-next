import ErrorButton from "@/components/error-demo/ErrorButton";

const Page = () => {
    return (
        <section>
            <h1>Throw error intentionally in /app/error-demo</h1>
            <ErrorButton/>
        </section>
    );
};

export default Page;