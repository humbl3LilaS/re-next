import ErrorButton from "@/components/error-demo/ErrorButton";

const Page = () => {
    return (
        <section>
            <h1>Throw error intentionally /app/error-demo-uncaught</h1>
            <ErrorButton/>
        </section>
    );
};

export default Page;