const Loading = () => {
    return (
        <section className="w-screen h-screen flex items-center justify-center">
            <h1 id={"text-3xl font-bold text-center"}>
                Loading............
            </h1>
        </section>
    );
};

export default Loading;